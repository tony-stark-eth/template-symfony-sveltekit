APP_ENV=dev
APP_SECRET=changeme_generate_with_openssl_rand_hex_32
DATABASE_URL="postgresql://app:!ChangeMe!@pgbouncer:5432/app?serverVersion=17&sslmode=disable&charset=utf8"
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=true
JWT_PASSPHRASE=changeme
JWT_TOKEN_TTL=3600
# OpenTelemetry is disabled in dev by default (no OTEL_PHP_AUTOLOAD_ENABLED).
# Prod OTel config is in compose.prod.yaml environment block.
