# compose.prod.yaml — Production overrides
#
# Usage:
#   docker compose -f compose.yaml -f compose.prod.yaml up -d
#
# Requires .env.local with: DATABASE_URL, APP_SECRET, MERCURE_JWT_SECRET,
# JWT_PASSPHRASE, MAILER_DSN, SENTRY_DSN, SERVER_NAME, POSTGRES_PASSWORD, etc.

services:
  php:
    image: app-php:prod
    build:
      target: frankenphp_prod
    env_file:
      - .env.local
    environment:
      - APP_ENV=prod
      - APP_DEBUG=0
      - FRANKENPHP_CONFIG=worker ./public/index.php
      # Override dev DATABASE_URL from compose.yaml (env_file loses to compose environment)
      - DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD:-!ChangeMe!}@pgbouncer:5432/app?serverVersion=17&sslmode=disable&charset=utf8
      - MERCURE_URL=https://php/.well-known/mercure
      - MERCURE_PUBLIC_URL=/.well-known/mercure
      - DEPLOY_SHA=${DEPLOY_SHA:-dev}
    restart: unless-stopped
    # Override dev bind mounts. Mount Caddy TLS/config volumes, JWT keys
    # (the prod image only contains .gitkeep), and Caddyfile (so config
    # changes take effect without rebuilding the image).
    volumes: !override
      - caddy_data:/data
      - caddy_config:/config
      - ./backend/config/jwt:/app/config/jwt:ro
      - ./docker/frankenphp/Caddyfile:/etc/caddy/Caddyfile:ro
      - /var/log/caddy:/var/log/caddy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 60s
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"  # HTTP/3
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  database:
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
    volumes:
      - /mnt/data/postgres:/var/lib/postgresql/data

  pgbouncer:
    environment:
      DB_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}

  messenger-worker:
    image: app-php:prod
    build:
      target: frankenphp_prod
    env_file:
      - .env.local
    # Override dev bind mounts — prod uses image-baked code
    volumes: !override []
    environment:
      - APP_ENV=prod
      - APP_DEBUG=0
      # Messenger worker connects directly to Postgres (LISTEN/NOTIFY)
      - DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/app?serverVersion=17&sslmode=disable&charset=utf8
    # Worker doesn't serve HTTP — disable inherited health check
    healthcheck:
      disable: true
    restart: unless-stopped

  # Mailpit is dev-only — prod uses an external SMTP provider via MAILER_DSN in .env.local
  mailpit:
    deploy:
      replicas: 0

volumes:
  caddy_data:
  caddy_config:
